# Netlify production build variables. Set this in Netlify, not in the browser.
# Netlify uses this value to generate the same-origin /api proxy redirect.
REACT_APP_API_URL=https://<railway-backend-domain>/api

# Keep database and Supabase credentials out of frontend env files.
# The React app only needs the public backend API URL.
