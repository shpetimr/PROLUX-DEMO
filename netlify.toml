[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "build"

[build.environment]
  CI = "true"
  PUBLIC_URL = "/"

# Production API proxy redirects are generated into frontend/build/_redirects
# from REACT_APP_API_URL during Netlify builds.
