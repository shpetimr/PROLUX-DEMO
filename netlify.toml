[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "build"

[build.environment]
  CI = "true"
  PUBLIC_URL = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
